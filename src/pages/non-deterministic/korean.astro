---
import Page from "../../layouts/Page.astro";
import CategoryTabs from "../../components/CategoryTabs.astro";
import LeaderboardTable from "../../components/LeaderboardTable.tsx";
import CurationNote from "../../components/CurationNote.astro";
import { buildView } from "../../lib/loadData.ts";
import "../../styles/global.css";

const view = await buildView("nd_korean");
---

<Page title="비결정적 — Korean">
  <CategoryTabs active="nd-korean" />
  <p class="text-sm text-neutral-500 mb-4">
    LLM 심판/사람 선호 기반 한국어 평가 — 매 실행마다 결과가 달라집니다: SEAL Korean, Ko Arena Hard 등.
  </p>
  <CurationNote hidden={view.hiddenBenchmarks} allHref="/non-deterministic" />
  <LeaderboardTable
    client:load
    benchmarks={view.benchmarks}
    rows={view.rows}
  />
</Page>
